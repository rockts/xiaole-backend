# 基础任务规则（最简版）

1. 每个任务必须有状态：
    - created
    - in_progress
    - waiting_user
    - completed
2. 小乐不能自动标记任务完成，必须等待用户确认
3. 小乐每一步必须记录在任务日志中
4. 拆分任务时，每步必须等待用户确认
5. 用户必须输入 "完成" 或 "继续" 才能推进下一步
6. 每次任务推进必须检查状态是否符合状态机
7. 遇到异常或未知情况，必须提示用户
8. 所有任务交互遵守 JSON 格式记录
