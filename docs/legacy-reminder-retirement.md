# 旧网页提醒退役与回滚

旧 PostgreSQL `reminders` 表仅作为历史数据保留。运行时不再注册旧提醒 API、工具或调度任务，也不再向小乐网页发送旧提醒弹窗事件。统一提醒能力由小乐 2.0 通过 Action Core 提供。

## 备份

- 导出时间：2026-08-21 18:32（Asia/Shanghai）
- 本机路径：`/Users/rockts/Dev/xiaole/backups/legacy-reminders-20260821-183130/reminders.csv`
- 数据量：27 条（CSV 共 28 行，含表头）
- 文件权限：`0400`
- SHA-256：`3770eb2862fd19634edefc3c4220c5750668c912ab3948f321d8acfe5559ad56`
- 关联任务的旧提醒：2 条

备份包含私人提醒内容，不得提交到 Git、复制到公开位置或用于自动迁移。恢复前必须再次获得人工授权。

## 回滚

1. 回滚前后端到本次退役提交之前的版本并重新部署。
2. 核对 `reminders` 表仍然存在且记录数与退役前盘点一致；正常情况下无需恢复数据。
3. 只有在确认表数据损坏或缺失后，才可在人工授权下从 CSV 恢复到隔离环境，核对主键和记录数后再决定是否恢复生产。
4. 恢复旧 scheduler 前，必须先暂停 Action Core 中对应提醒，避免双重投递。

禁止直接清空 `reminders` 表，也禁止批量把旧记录标记为已迁移。

现有 `reminders.task_id` 外键使用 `ON DELETE CASCADE`。本次未修改数据库结构；应用层会拒绝删除与旧提醒关联的任务。解除该保护或直接操作数据库必须单独获得人工授权，并先核对只读备份。
